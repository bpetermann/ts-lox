fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2); 
}

var before = clock();
fib(40);
var after = clock();
var time = (after - before) / 1000;

print "🧪 execute fibonacci 40 times in...";
print time + " s";